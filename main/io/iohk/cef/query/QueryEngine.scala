package io.iohk.cef.query

/** Marker trait to signal that, whatever implements this trait will provide the low level operations that
    would allow access to the data needed to perform the queries */
trait QueryEngine
