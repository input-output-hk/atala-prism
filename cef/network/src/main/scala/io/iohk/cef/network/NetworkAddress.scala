package io.iohk.cef.network

/**
  * A network address represents the an addressable identity on cef network.
  * @param idHex a unique number, hex encoded.
  */
case class NetworkAddress(idHex: String)
