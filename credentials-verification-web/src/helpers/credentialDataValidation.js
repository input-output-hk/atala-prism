// TODO: implement credentials data validation

// Credential data bulk-import validations
export const validateCredentialDataBulk = (contacts, inputHeaders) => ({
  validationErrors: [[]],
  containsErrors: false
});
