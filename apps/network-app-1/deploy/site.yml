---
- hosts: all
  gather_facts: False
  become: True
  tasks:
  - name: install python 2
    raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)

- hosts: control-node
  roles:
#    - common
    - control-node

- hosts: p2p-net
  roles:
#    - common
    - p2p-node
