<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="io.iohk.atala.prism.app.neo.ui.onboarding.walletsetup.WalletSetupViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ScrollView
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:paddingBottom="50dp"
            android:clipToPadding="false"
            app:layout_constraintBottom_toTopOf="@+id/accept_button"
            android:layout_width="match_parent"
            android:layout_height="0dp">

            <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:paddingStart="24dp"
                    android:paddingEnd="24dp"
                    android:id="@+id/text_view_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="48dp"
                    android:text="@string/wallet_setup_activity_title"
                    android:textAlignment="textStart"
                    android:textColor="@color/black"
                    android:textSize="28sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_marginStart="24dp"
                    android:layout_marginEnd="24dp"
                    android:id="@+id/text_view_subtitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/seed_phrase_description"
                    android:textAlignment="textStart" />

                <androidx.recyclerview.widget.RecyclerView
                    android:paddingStart="20dp"
                    android:paddingEnd="20dp"
                    android:id="@+id/recycler_view_seed_phrase"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:backgroundTint="@color/white"
                    android:clickable="false"
                    android:contextClickable="false"
                    android:paddingTop="8dp"
                    android:paddingBottom="8dp" />

                <TextView
                    android:layout_marginStart="24dp"
                    android:layout_marginEnd="24dp"
                    android:id="@+id/text_view_description"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="@string/seed_phrase_explanation"
                    android:textAlignment="textStart" />

                <include
                    android:layout_marginStart="24dp"
                    android:layout_marginEnd="24dp"
                    checked="@={viewModel.mnemonicListAccepted}"
                    layout="@layout/neo_include_checkbox_with_description"
                    text="@{@string/seed_phrase_acceptance}"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp" />

            </LinearLayout>

        </ScrollView>


        <Button
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:id="@+id/accept_button"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_marginBottom="32dp"
            android:enabled="@{viewModel.acceptButtonEnabled}"
            android:onClick="@{(view) -> viewModel.acceptButtonTaped()}"
            android:text="@string/accept"
            app:cornerRadius="32dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>