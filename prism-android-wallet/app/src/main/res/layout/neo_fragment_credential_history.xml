<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="io.iohk.atala.prism.app.ui.main.credentials.CredentialHistoryViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="0dp"
            android:layout_marginHorizontal="0dp"
            android:background="@color/grey_2" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/materialCardView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/default_spacing"
            app:cardCornerRadius="@dimen/card_corner_radius"
            app:cardElevation="10sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/default_spacing">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/credential_logo"
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:layout_centerInParent="true"
                        android:scaleType="fitCenter"
                        app:srcCompat="@drawable/ic_conection_list"
                        tools:ignore="ContentDescription" />

                    <TextView
                        android:ellipsize="end"
                        android:layout_gravity="center_vertical"
                        android:id="@+id/credential_title_text_view"
                        android:layout_marginStart="@dimen/internal_margin"
                        android:layout_weight="1"
                        android:layout_width="0dp"
                        android:maxLines="2"
                        android:textSize="15sp"
                        android:layout_height="wrap_content"
                        android:text="@{viewModel.credential.issuerName}"
                        android:textColor="@color/black"
                        android:textStyle="bold" />
                </LinearLayout>

                <FrameLayout
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:layout_marginTop="@dimen/divider_margin"
                    android:layout_marginBottom="@dimen/divider_margin"
                    android:background="@color/grey_2" />

                <include
                    layout="@layout/neo_include_message_box_success"
                    app:iconSrc="@{@drawable/ic_clock}"
                    app:text="@{@string/received(viewModel.formattedIssuedDate)}" />
            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <androidx.recyclerview.widget.RecyclerView
            android:clipToPadding="false"
            android:id="@+id/activity_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:paddingTop="@dimen/default_spacing"
            android:paddingEnd="@dimen/default_spacing"
            android:paddingStart="@dimen/default_spacing"
            android:paddingBottom="100dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/materialCardView" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>